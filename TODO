* Change to more-conventional malloc'ing?  Mischief managed.

* BPF for Linux now that 2.1.x has it.

* SIGUSR2 & SIGHUP sometimes cause endless output looping.

* Finish autoconf.

* Add native command-line address selection.

* Add an rc file capability for default (no argument) usage: ~/.issniffrc

* Add console front-end option.

* Add output tty selection.

* Other things, such as IPC and encrypted transfers.
